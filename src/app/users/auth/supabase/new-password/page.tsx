import { SupabaseNewPasswordView } from '../authSection';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Supabase: New Password',
};

export default function NewPasswordPage() {
  return <SupabaseNewPasswordView />;
}

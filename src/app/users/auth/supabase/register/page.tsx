import { SupabaseRegisterView } from '../authSection';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Supabase: Register',
};

export default function RegisterPage() {
  return <SupabaseRegisterView />;
}

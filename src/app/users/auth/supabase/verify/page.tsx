import { SupabaseVerifyView } from '../authSection';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Supabase: Verify',
};

export default function VerifyPage() {
  return <SupabaseVerifyView />;
}
